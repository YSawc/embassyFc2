package ModePkg {
    enum CpuMode: logic<8> {
        nop,
        normal,
        debug, // [mode, subcommand]
    }

    enum OpeMode: logic<3> {
        Nop,
        ReadAndStoreMemory,
        ReadAndStoreToRegister,
        StoreMemory,
        Other,
    }
}
